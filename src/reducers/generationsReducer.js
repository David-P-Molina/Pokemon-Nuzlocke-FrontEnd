function GenerationsReducer(
    state = {
        generations: [],
    }, action 
) {
    switch (action.type) {

        default: 
            return state
    }
}

export default GenerationsReducer